# StudyPilot 项目部署说明

## 1. 适用范围

本文用于首次取得 StudyPilot 源码后的本地部署与答辩演示。推荐在 Windows 10/11 上通过 Docker Compose 部署完整技术栈。

- 项目：StudyPilot 智能学习助手；
- 工作分支：`liverharden`；
- 文档整理基线：`eb71a9ba53ba77d6238cbd11674efc76ea37e553`；
- 支持环境：Windows 10/11 64 位、Docker Desktop（WSL2 后端）；
- 推荐方式：Docker Compose。

## 2. 服务组成

| 服务 | 作用 | 默认主机端口 |
|---|---|---:|
| `frontend` | Vue 页面与 Nginx 反向代理 | 8080 |
| `backend` | FastAPI 接口、认证、RAG 和业务服务 | 8000 |
| `worker` | Celery 文档解析与后台增强任务 | 无 |
| `postgres` | 业务数据与 pgvector 向量数据 | 5432 |
| `redis` | 缓存、Celery Broker 与结果后端 | 6379 |

文档解析、周报和可选 AI 增强依赖 Redis 与 `worker`。因此启动时必须运行完整 Compose 栈，不能只启动前端和后端。

## 3. 环境要求

- Windows 10/11 64 位；
- Docker Desktop，且 Docker Engine 已启动；
- WSL2（Docker Desktop 的 Linux 容器后端）；
- Git；
- 建议至少 8 GB 内存、20 GB 可用磁盘空间；
- 端口 `5432`、`6379`、`8000`、`8080` 未被其他程序占用。

在 PowerShell 中确认 Docker 可用：

```powershell
docker --version
docker compose version
docker info
```

## 4. 获取源码

可克隆仓库，或解压源码压缩包后进入项目根目录。不要在说明中依赖某台电脑的绝对路径。

```powershell
git clone https://github.com/C-Yhen/Summer_practice_studying_assistant.git
cd Summer_practice_studying_assistant
```

## 5. 配置环境变量

首次运行复制模板：

```powershell
Copy-Item .env.example .env
```

请在本机 `.env` 中设置或检查以下字段，绝不将真实值提交到 Git：

| 字段 | 用途 |
|---|---|
| `JWT_SECRET` | 登录 Token 签名密钥，部署时改为长随机值 |
| `POSTGRES_PASSWORD` | PostgreSQL 密码 |
| `LLM_PROVIDER` | AI Provider，`mock` 或 `qwen` |
| `LLM_API_KEY` | Qwen API Key，仅本机保存 |
| `LLM_CHAT_MODEL` | 对话与结构化增强模型 |
| `LLM_EMBEDDING_MODEL` | 资料检索向量模型 |

### 离线/基础演示模式

设置 `LLM_PROVIDER=mock`。该模式可演示完整页面与基础业务链路，不需要 API Key；AI 相关内容仅用于链路演示，不应视为真实模型结果。

### 真实 Qwen 模式

`.env.example` 给出了 DashScope OpenAI 兼容端点与模型名称示例。要使用真实 Qwen，填写新的本地密钥：

```dotenv
LLM_PROVIDER=qwen
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_API_KEY=在此处填写仅本机使用的新密钥
LLM_CHAT_MODEL=qwen3.7-plus
LLM_EMBEDDING_MODEL=qwen3.7-text-embedding
```

切换 Embedding Provider 或模型后，应在资料页面对既有资料执行“重新解析”，使当前文档版本重新生成与当前 Provider 一致的向量；当前数据库向量维度为 1024，不能只改维度配置而不同时修改模型与索引。

## 6. 首次构建与启动

在项目根目录执行：

```powershell
docker compose config --quiet
docker compose up -d --build
docker compose ps
```

也可双击根目录的 `start.bat`。脚本会检查 Docker、缺失的 `.env`、Compose 配置并启动全部服务。

## 7. 启动成功判断

`docker compose ps` 中，`postgres`、`redis`、`backend` 应显示 `healthy`，`frontend`、`worker` 应显示 `Up`。访问：

- 前端：<http://localhost:8080>
- 后端健康检查：<http://localhost:8000/health>
- Swagger：<http://localhost:8000/docs>
- OpenAPI：<http://localhost:8000/openapi.json>

也可执行：

```powershell
Invoke-RestMethod http://localhost:8000/health
(Invoke-WebRequest http://localhost:8080 -UseBasicParsing).StatusCode
```

## 8. 第一次使用前准备

1. 打开前端并注册账号；
2. 创建课程；
3. 上传 PDF、TXT 或 Markdown 资料；
4. 等待资料处理状态为 `ready`；
5. 再使用资料问答、计划、练习和推荐。

后端直接支持 PDF、TXT、`.md`、`.markdown`。PPT/PPTX 请先从 PowerPoint 导出为 PDF；前端也支持一次选择 ZIP 并解压其中的受支持资料。

## 9. 停止与再次启动

普通停止（保留数据库、Redis 与上传资料卷）：

```powershell
docker compose down
```

再次启动：

```powershell
docker compose up -d
```

`stop.bat` 只封装普通停止。**不要**在普通停止时运行 `docker compose down -v`：该命令会删除数据库、Redis 与上传资料卷。

## 10. 常见故障排查

| 现象 | 排查方式 |
|---|---|
| `docker` 无法识别 | 启动 Docker Desktop，重新打开 PowerShell，并确认 Docker CLI 已加入 PATH。 |
| 无法连接 Docker daemon | 执行 `docker info`，确认 Docker Desktop 显示 Engine running。 |
| 端口被占用 | 检查 `5432/6379/8000/8080`，停止冲突程序或调整 Compose 主机端口。 |
| `backend` unhealthy | 执行 `docker compose logs --tail=200 backend postgres redis`；已有卷密码与新 `.env` 不一致时，优先恢复原密码或备份数据。 |
| 文档长期 `queued/processing` | 执行 `docker compose ps worker redis` 与 `docker compose logs --tail=200 worker redis`，确认 Worker 和 Redis 正常。 |
| 前端提示后端不可用 | 先访问 `/health`，再看 `docker compose logs --tail=100 frontend backend`。生产前端不会静默回退 Mock。 |
| Qwen 调用失败 | 检查本机 `LLM_API_KEY`、Provider、网络和 Worker 日志；规则优先流程仍可提供部分基础结果。 |
| 上传后不能问答 | 确认资料为 `ready`，并在切换 Embedding Provider 后重新解析。 |
| 修改代码后未生效 | 执行 `docker compose up -d --build backend worker frontend`。 |

详细的 Windows 与 Docker 维护说明见 [docs/07-docker-setup.md](./docs/07-docker-setup.md)。

## 11. 安全与提交注意事项

不得提交 `.env`、真实 API Key、JWT Secret、数据库密码、上传资料、本地数据库、`node_modules`、`.venv`、日志或临时文件。`.env.example` 只能作为示例配置。
