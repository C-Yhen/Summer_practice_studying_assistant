# 项目状态

## Round 01：真实认证闭环

### 已完成

- 打通真实注册、登录、JWT 保存、`/users/me` 会话恢复和退出登录。
- 统一 API Envelope 解包、用户字段转换和认证错误处理。
- Mock 只在 `VITE_ENABLE_MOCK=true` 时开启，真实模式不伪造 Token 或用户。
- 路由守卫等待会话初始化，页面用户名来自后端响应。

### 实际测试

- `npm install`：成功，114 个包，0 vulnerabilities。
- `npm run build`：通过。
- 后端认证测试：3 passed；完整后端套件：8 passed。
- 真实 API：注册 201、登录 200、`/users/me` 200、错误密码 401、重复注册 409、校验失败 422、无效/篡改 Token 401。

### 未解决问题

- 尚无前端浏览器 E2E 回归测试。
- 尚未实现全局 401 拦截、Token 刷新、忘记/修改密码。
- 其他业务模块未在本轮改造。

### 下一轮候选

- 增加认证 E2E 回归，覆盖刷新恢复、Token 篡改和退出。
- 评估全局 401 会话失效处理。

## Round 02：真实课程列表与创建

### 已完成

- 接入真实 `GET /courses` 和 `POST /courses`，正确处理 `{ items, total }`。
- 集中定义/转换课程字段，实现加载、空状态、错误和防重提交。
- 创建成功后立即更新列表；移除教师、资料数、知识点数和掌握度等伪数据。
- 显式 Mock 模式保留同构列表与内存创建行为。

### 实际测试

- `npm run build`：通过。
- 完整后端套件：9 passed，包含创建/列表、用户隔离、未登录拒绝和空白名称校验。
- 真实 API 已完成注册、登录、创建与两次独立列表查询，新课程均存在。

### 未解决问题

- 课程详情、编辑、归档和恢复未在本轮实现。
- 当前列表不展示后端未提供的资料、知识点和掌握度统计。

### 下一轮候选

- 评估真实课程详情读取闭环。
- 增加课程列表/创建的前端 E2E 回归。

## Round 03：真实文档上传与处理状态

### 已完成

- 接入真实单文件上传、课程文档列表、文档详情和 REST 任务轮询。
- 使用真实课程/文档/任务 ID 恢复页面；移除固定文档、假进度和假 WebSocket 状态。
- 加固空文件、空文本、残缺文件清理与失败状态，并对齐 Backend/Worker 配置和共享上传卷。

### 实际测试

- `npm run build`：通过；完整后端套件：13 passed。
- 同步模式：TXT/MD 落盘、文档/版本/任务/文本块持久化、失败状态及权限/类型/大小校验通过。
- Celery 模式：真实 TXT 上传后任务 `success`、文档 `ready`，生成 1 页/1 块；刷新列表仍存在，Backend/Worker 均可读取文件。
- 后端关闭时前端代理返回 502，未回退 Mock；不支持文件返回 415。

### 未解决问题

- 尚无浏览器自动化 E2E；本轮未联调 PDF 解析和真实外部 Embedding 服务。
- 删除、重新解析、版本切换及聊天问答不在本轮范围。

### 下一轮候选

- 增加文档上传、刷新恢复和任务失败展示的前端 E2E 回归。
- 评估真实课程详情读取闭环。
