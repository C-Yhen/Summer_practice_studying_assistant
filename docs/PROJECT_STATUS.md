# 项目状态

## Round 01：真实认证闭环

### 已完成

- 打通真实注册、登录、JWT 保存、`/users/me` 会话恢复和退出登录。
- 统一 API Envelope 解包、用户字段转换和认证错误处理。
- Mock 只在 `VITE_ENABLE_MOCK=true` 时开启，真实模式不伪造 Token 或用户。
- 路由守卫等待会话初始化，页面用户名来自后端响应。

### 实际测试

- `npm install`：成功，114 个包，0 vulnerabilities。
- `npm run build`：通过。
- 后端认证测试：3 passed；完整后端套件：8 passed。
- 真实 API：注册 201、登录 200、`/users/me` 200、错误密码 401、重复注册 409、校验失败 422、无效/篡改 Token 401。

### 未解决问题

- 尚无前端浏览器 E2E 回归测试。
- 尚未实现全局 401 拦截、Token 刷新、忘记/修改密码。
- 其他业务模块未在本轮改造。

### 下一轮候选

- 增加认证 E2E 回归，覆盖刷新恢复、Token 篡改和退出。
- 评估全局 401 会话失效处理。

## Round 02：真实课程列表与创建

### 已完成

- 接入真实 `GET /courses` 和 `POST /courses`，正确处理 `{ items, total }`。
- 集中定义/转换课程字段，实现加载、空状态、错误和防重提交。
- 创建成功后立即更新列表；移除教师、资料数、知识点数和掌握度等伪数据。
- 显式 Mock 模式保留同构列表与内存创建行为。

### 实际测试

- `npm run build`：通过。
- 完整后端套件：9 passed，包含创建/列表、用户隔离、未登录拒绝和空白名称校验。
- 真实 API 已完成注册、登录、创建与两次独立列表查询，新课程均存在。

### 未解决问题

- 课程详情、编辑、归档和恢复未在本轮实现。
- 当前列表不展示后端未提供的资料、知识点和掌握度统计。

### 下一轮候选

- 评估真实课程详情读取闭环。
- 增加课程列表/创建的前端 E2E 回归。

## Round 03：真实文档上传与处理状态

### 已完成

- 接入真实单文件上传、课程文档列表、文档详情和 REST 任务轮询。
- 使用真实课程/文档/任务 ID 恢复页面；移除固定文档、假进度和假 WebSocket 状态。
- 加固空文件、空文本、残缺文件清理与失败状态，并对齐 Backend/Worker 配置和共享上传卷。

### 实际测试

- `npm run build`：通过；完整后端套件：13 passed。
- 同步模式：TXT/MD 落盘、文档/版本/任务/文本块持久化、失败状态及权限/类型/大小校验通过。
- Celery 模式：真实 TXT 上传后任务 `success`、文档 `ready`，生成 1 页/1 块；刷新列表仍存在，Backend/Worker 均可读取文件。
- 后端关闭时前端代理返回 502，未回退 Mock；不支持文件返回 415。

### 未解决问题

- 尚无浏览器自动化 E2E；本轮未联调 PDF 解析和真实外部 Embedding 服务。
- 删除、重新解析、版本切换及聊天问答不在本轮范围。

### 下一轮候选

- 增加文档上传、刷新恢复和任务失败展示的前端 E2E 回归。
- 评估真实课程详情读取闭环。

## Round 04：真实 RAG 问答与会话持久化

### 已完成

- 问答页接入真实课程、已就绪文档范围、会话列表、消息历史、RAG 回答与引用，URL 可恢复课程和会话。
- 后端补齐会话列表、首问标题、更新时间、消息证据字段、文档范围校验及 Provider/事务失败处理。
- 显式 Mock 仅保留内存演示路径；真实模式不再使用 demo 会话、静态回答或假引用。

### 实际测试

- `npm run build`：通过；完整后端套件：17 passed。
- Docker 真实链路：注册、课程、TXT 异步处理、相关/无关问题、引用、重复读取、双会话切换和跨用户隔离通过。
- 无头 Chrome：真实课程/回答/引用显示、URL 恢复、刷新恢复、证据不足和双会话切换通过。
- 离线 Mock Provider 已验证；外部 Chat 与 Embedding Provider 未验证。

### 未解决问题

- 尚未联调外部模型；暂无纳入仓库的前端 E2E 测试框架。
- 会话删除、重命名、分页、流式输出和原文定位不在本轮范围。

### 下一轮候选

- 评估真实课程详情闭环，或将关键浏览器路径纳入轻量自动化回归。

## Round 05：真实学习计划、确认与今日任务

### 已完成

- 接通真实课程计划生成、候选版本展示、刷新恢复确认令牌和受控确认。
- 今日任务仅读取当前活动版本；候选及旧版本任务不可见且不可完成。
- 完成任务持久化实际分钟、学习记录和掌握度，重复提交保持幂等。
- 计划和今日任务页移除固定日期、版本、统计、计时与延期等假功能；显式 Mock 保留内存演示路径。

### 实际测试

- `npm run build`：通过；完整后端套件：21 passed。
- PostgreSQL 真实链路：候选隔离/刷新确认、活动任务、完成刷新、幂等、学习记录、掌握度及跨用户隔离通过。
- 无头 Chrome：页面生成、候选刷新、确认、活动计划刷新、今日任务和完成刷新通过。

### 未解决问题

- 外部模型仍未联调；暂无纳入仓库的前端 E2E 测试框架。
- 动态调整、任务延期、专注计时、计划历史和日历同步不在本轮范围。

### 下一轮候选

- 将计划确认和任务完成的浏览器路径纳入轻量自动化回归，或评估真实课程详情闭环。

### 并发幂等补修

- PostgreSQL 完成任务时使用任务行锁，并在锁后按最新状态返回幂等结果。
- 为非空 `LearningRecord.task_id` 增加唯一约束和现有数据库启动迁移，防止重复完成记录。
- 实测两个独立数据库会话并发完成：仅 1 条记录、掌握度仅更新 1 次，首次实际分钟数保持不变。
- `npm run build` 通过；完整后端测试 22 passed。

## Round 06：真实首页仪表盘数据

### 已完成

- 新增只读 `GET /dashboard/overview`，按当前用户、目标日期、时区和焦点课程聚合课程、就绪资料、生效计划任务、学习记录、掌握度与异步任务。
- 首页移除固定课程、假统计、假推荐和本地任务勾选，接入类型校验、加载/错误/空状态、真实趋势、薄弱点、规则化下一步和快捷入口。
- 显式 Mock 返回同构演示数据；真实模式请求失败不会回退 Mock。

### 实际测试

- `npm run build`：通过；完整后端测试：26 passed（仪表盘新增 4 个场景组）。
- PostgreSQL：新账号空首页；创建课程、上传并就绪、确认计划、完成任务后，任务 0/1→1/1，专注/趋势更新为 27 分钟并生成真实掌握度。
- 无头 Chrome：真实账号首页、刷新持久化、图表、新账号空状态通过；后端关闭时显示服务错误且无演示数据。

### 未解决问题

- 尚未把浏览器检查纳入仓库内长期 E2E 套件；趋势只统计查询时仍属当前生效计划版本的任务。
- 课程详情、全局统计和推荐中心仍不在真实仪表盘闭环范围内。

### 下一轮候选

- 评估真实课程详情闭环，或将关键首页路径纳入轻量浏览器回归。

## Round 07：真实课程详情与课程上下文一致性

### 已完成

- 课程详情按 URL 读取真实课程，并分别加载课程限定的 Dashboard、资料、掌握度和学习记录；非法、不存在、跨用户及已归档课程不回退其他数据。
- 上传、问答、计划、今日任务、掌握度和文档任务页保持真实 `courseId`；多课程切换会清除上一课程状态。
- 接通真实编辑、归档、文档处理状态与重新解析；掌握度增加 `has_record`，学习记录补充可空的任务和知识点名称。
- 补修文档与异步任务归属：新增文档限定任务详情接口，页面初始化和轮询均验证 `courseId → documentId → taskId` 链路。

### 实际测试

- `npm run build` 通过；完整后端测试 29 passed。
- PostgreSQL 与无头 Chrome：课程 A/B 详情隔离、刷新、五个课程入口、浏览器真实上传及任务 URL、编辑持久化、非法/跨用户 404、归档后列表移除均通过。
- 后端关闭时显示服务错误，不出现 Mock 或其他课程。
- 本次补修：`npm run build` 通过；完整后端测试 30 passed，包含同课程/跨课程/跨用户、非文档任务、归档课程和已删除文档的任务错配 404。

### 未解决问题

- 浏览器检查尚未纳入长期 E2E 套件；掌握度模型没有历史快照，因此趋势诚实返回暂无。
- 当前执行环境没有可用受控浏览器，无法实际操作验证本次手工替换 `taskId` 的页面 URL；后端资源链与前端构建已验证。
- Harness、LangChain、MCP、第三方服务、缓存、定制计划、推荐系统和长时任务增强均保留为后续候选，本轮未实现。

## Round 08：真实长时任务中心与 Celery 执行闭环

### Round 08 补修：周报范围、时区与取消一致性

- 周报任务仅统计当前用户、当前 active 计划及 active 版本的任务；candidate 和 superseded 版本均排除。
- 周报与 Dashboard 共用用户时区工具，以本地日期转换的 UTC 左闭右开范围统计学习记录和学习天数；无效时区回退 UTC。
- 写入周报成功结果前再次检查取消请求；周报重试安全转换课程资源 ID，并继续校验归属和归档状态。
- 实际测试：定向异步任务测试 7 passed；完整后端测试 37 passed；`frontend npm run build` 通过。

## Round 09：真实可解释推荐中心

- 新增只读统一课程推荐，使用 active 计划任务、真实掌握度、就绪资料、考试日期和学习记录生成规则化解释。
- 推荐查询不再写入历史；点击、有帮助和不感兴趣反馈按课程与推荐目标幂等持久化，并提供真实历史与交互计数。
- 前端推荐中心接入真实课程上下文、推荐、反馈和历史；显式 Mock 保留独立演示路径。
- 实际测试：`frontend npm run build` 通过；完整后端测试 39 passed。浏览器联调未执行（当前无可用受控浏览器）。

### Round 09 补修：推荐归属与反馈状态

- 推荐查询约束 KnowledgePoint 必须属于当前课程，防止不一致的掌握度或任务关联泄漏到推荐中。
- 推荐卡片反馈成功后仅更新当前卡片状态；切换课程清空状态，历史弹窗打开时同步刷新。
- 实际测试：`frontend npm run build` 通过；完整后端测试 39 passed。

### 已完成

- 真实任务中心支持 `document_parse` 与 `weekly_report`：列表/详情、筛选分页、REST 自动刷新、URL 任务恢复、真实取消和重试。
- 统一任务派发层按类型调用文档 Worker 或周报 Worker；未实现类型明确拒绝，派发失败持久化为 `failed`。
- 周报基于当前用户真实学习记录、计划任务、课程和掌握度聚合；同步模式与 Celery 模式复用业务服务。
- 扩充任务响应中的资源、输入、时间、结果、取消/重试能力字段；文档限定任务接口复用同一序列化结果。

### 实际测试

- `npm run build` 通过；完整后端测试：33 passed。
- PostgreSQL + Redis + Celery Worker 联调：真实上传文档和创建课程周报均执行成功，结果持久化，刷新任务列表仍有两项。
- 浏览器运行时无可用实例，未能执行任务中心页面交互验证。

### 未解决问题

- 浏览器联调及长期前端 E2E 套件仍未具备可用运行环境。
- Harness、LangChain、MCP、外部模型、第三方日历、业务缓存、定制计划和推荐系统仍保留为后续候选。

### 下一轮候选

- 将多课程上下文关键路径纳入轻量 E2E，或按老师优先级评估上述工程能力。
