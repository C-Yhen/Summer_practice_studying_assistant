# StudyPilot 快速启动指南

## 环境准备

1. 安装并启动 Docker Desktop；
2. 确认 Docker Engine 正常：`docker info`；
3. 获取并解压或克隆项目源码，进入项目根目录。

## 首次启动

如果当前电脑已经有本机 `.env`，直接保留并使用它，`start.bat` 不会覆盖该文件。老师或其他人员首次获得源码时，源码包不包含 `.env` 和真实密钥，需要执行：

```powershell
Copy-Item .env.example .env
docker compose up -d --build
```

真实 Qwen 模式请在自己的 `.env` 填写 `LLM_API_KEY`；没有 Qwen Key 时，将 `LLM_PROVIDER` 改为 `mock`。不要索取、打印或提交项目作者的个人 API Key。

也可双击根目录的 `start.bat`。首次构建需要下载镜像和依赖，耗时会比后续启动更长。

## 访问地址

- 前端：<http://localhost:8080>
- 后端健康检查：<http://localhost:8000/health>
- API 文档：<http://localhost:8000/docs>

## 第一次使用

1. 注册账号；
2. 创建课程；
3. 上传 PDF、TXT 或 Markdown 资料；
4. 等待资料处理为 `ready`；
5. 开始使用问答、计划和练习。

## 停止项目

```powershell
docker compose down
```

或双击 `stop.bat`。此命令保留数据卷；不要使用 `docker compose down -v`，否则会删除本地数据库和上传资料。

更多信息见：[项目部署说明](./项目部署说明.md) 与 [项目使用说明](./项目使用说明.md)。
